[project]
name = "cli"
version = "0.1.0"
description = "CLI Application for uploading JATS documents"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jats-classes",
    "jats-exporters",
    "jats-importexport-client",
    "typer>=0.12",
    "rich>=13.0",
]

[project.scripts]
jats-cli = "cli.main:main"
jats-validate = "cli.main:validate_main"
jats-validate-remote = "cli.main:validate_remote_main"
jats-validate-all = "cli.main:validate_all_main"

[dependency-groups]
dev = [
    "ruff>=0.9",
    "ty>=0.0.1",
]

[tool.uv.sources]
jats-classes = {workspace = true}
jats-exporters = {workspace = true}
jats-importexport-client = {workspace = true}

[tool.uv]
package = true

[build-system]
requires = ["uv_build>=0.11.16,<0.12"]
build-backend = "uv_build"
